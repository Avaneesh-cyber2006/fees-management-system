# WhatsApp Reminder System Dependencies
pandas==2.1.1
pymysql==1.1.0
selenium>=4.15.0
openpyxl==3.1.2

# Note: datetime, time, pathlib, csv, os are built-in Python modules
# and don't need to be installed via pip

# Existing project dependencies (if not already installed)
Django==5.2.7
mysqlclient==2.2.0
reportlab==4.0.4
matplotlib==3.7.2
plotly==5.17.0
